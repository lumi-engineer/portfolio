# Resume download

Place your PDF here so the **RESUME DOWNLOAD** button in the About section works:

```
public/resume.pdf
```

The site links to `/resume.pdf` and suggests the filename `Lucas-Daniel-Resume.pdf` when saving.

To use a different path or filename, update `about.resumeUrl` and `about.resumeDownloadName` in `src/data/portfolio.ts`.
