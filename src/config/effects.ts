/**
 * Gold coin size in pixels (diameter). Change this value to resize cursor coins.
 */
export const GOLD_COIN_SIZE = 28;

/** How many coins spawn per mouse move (1–3 recommended). */
export const GOLD_COINS_PER_MOVE = 2;

/** Max coins on screen at once. */
export const GOLD_COIN_MAX_COUNT = 80;
